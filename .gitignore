# ── 依赖与构建产物 ─────────────────────────────────────────────
node_modules/
dist/
.astro/
*.tsbuildinfo

# ── 运行时数据（数据库 / 媒体 / 导出内容 / 发布产物）──────────
packages/server/data/
packages/site/content/data/
packages/site/public/media/
packages/site/public/uploads/
public-dist/
docker-data/

# ── 本地开发上下文（AI 协作文档，不入库）──────────────────────
.ai/
.hallmark/

# ── 环境与杂项 ─────────────────────────────────────────────────
.env
.env.*
*.log
.DS_Store

# 文楷自托管字体（构建时从 node_modules 拷贝）
packages/site/public/fonts/
